apply plugin: 'application'

mainClassName = 'com.cfhayes.patterns.singleton.Client'

jar {
  manifest {
    attributes(
      'Class-Path': configurations.compile.collect { it.getName() }.join(' '),
      'Main-Class': mainClassName
    )
  }
}
